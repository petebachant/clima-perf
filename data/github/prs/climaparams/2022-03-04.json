[
  {
    "additions": 115,
    "author": {
      "id": "MDQ6VXNlcjEwNjczMDcw",
      "is_bot": false,
      "login": "bielim",
      "name": ""
    },
    "body": "- `get_parameter_values` extracts the requested parameter values and returns\r\n  them as an array. The returned array contains the values corresponding\r\n  to the given list of parameter names (unless a single scalar  parameter is\r\n  requested, in which case the function returns a float)\r\n- The return type of `get_parameter_values` is determined by the type of the \r\n  `ParamStruct` the parameter values are extracted from. If e.g. the `ParamStruct`\r\n  is of type `Float64`, then `get_parameter_values` returns parameter values \r\n  of type `Float64`. Note that the list of parameter names passed to \r\n  `get_parameter_values` can include both array-valued and scalar-valued\r\n  parameters.\r\n- E.g., given a list of parameter names [\"scalar_param\", \"array_param\"] with\r\n  `scalar_param` having a scalar value and `array_param` being array-valued,\r\n  the first element of the returned array will be a float, and the second\r\n  element will be an array of floats\r\n- Added more tests to `toml_consistency.jl` to check if parsing, extracting,\r\n  and writing of array-valued parameters works. Also added a test for\r\n  `merge_override_default_values`",
    "changedFiles": 3,
    "closingIssuesReferences": [],
    "createdAt": "2022-03-03T01:50:23Z",
    "deletions": 25,
    "labels": [],
    "mergedAt": "2022-03-04T00:29:03Z",
    "number": 65,
    "title": "Enable get_parameter_values to deal with array-valued parameters"
  }
]